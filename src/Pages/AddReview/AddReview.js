import React from 'react';

const AddReview = () => {
    return (
        <div>
            AddReview
            AddReview
            AddReview
            AddReview
            AddReview
            AddReview
        </div>
    );
};

export default AddReview;