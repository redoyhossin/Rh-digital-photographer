import React from 'react';
import UseTitle from '../../Hooks/Usetitle';

const AddReview = () => {
    UseTitle('AddReview')
    return (
        <div>
            AddReview
            AddReview
            AddReview
            AddReview
            AddReview
            AddReview
        </div>
    );
};

export default AddReview;